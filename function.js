

function addvalues (x,y,z){  // the function takes in 3 arguments
    console.log(x + y + z);  // the function returns the sum of all 3 arguments
}

addvalues(12,3,4);   // the function is called and arguments are passed into it